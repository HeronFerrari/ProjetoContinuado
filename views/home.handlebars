<div class="text-center p-5 mb-4 bg-light rounded-3">
  <h1 class="display-4">Bem-vindo à Biblioteca Online!</h1>
  
  {{#if logado}}
    <p class="lead">Use o menu de navegação acima para explorar os recursos disponíveis para você.</p>
    <p>Seu tipo de usuário é: <strong>{{usuario.tipo}}</strong></p>
  {{else}}
    <p class="lead">Faça o login ou cadastre-se para acessar nossos recursos.</p>
    <div>
        <a href="/login" class="btn btn-primary btn-lg">Login</a>
        <a href="/usuarioCreate" class="btn btn-secondary btn-lg">Cadastre-se</a>
    </div>
  {{/if}}

  {{#if error}}
    <div class="alert alert-danger mt-4" role="alert">
      {{error}}
    </div>
  {{/if}}
</div>